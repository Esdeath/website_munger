# 思维格栅文章首段去重设计

## 目标

修复思维格栅模型页在标题下同时显示摘要和正文首段，造成开头重复的问题。

## 方案

保留页面标题下的 `reader-intro` 摘要，用于阅读入口和 SEO 描述；在渲染正文前，先移除 Markdown 标题以及紧随其后的第一个正文段落。模型快照文件保持原样，其他栏目不受影响。

## 验证

- 增加纯函数测试，确认标题与首段被移除、后续正文保留。
- 运行思维格栅相关 Vitest 测试。
- 运行仓库的 `npm run check` 完整门禁。
